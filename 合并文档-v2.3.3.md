# Atoll 上游 v2.3.3 合并文档

- 日期:2026-08-06
- 本地 fork:`chenchangzhong/Atoll`
- 上游:`Ebullioscopic/Atoll`
- 合并提交:`bb7e232`(Merge remote-tracking branch 'upstream/main')
- 合并方式:`git merge`,ort 策略,**零冲突**

---

## 1. 合并概况

| 项 | 值 |
|---|---|
| 上游范围 | `a2f0ffd`(v2.3.2 发布点)→ `246add1`(v2.3.3 + CI 归档) |
| 新 tag | `v2.3.3`、`v2.3.3-alpha.1` |
| 版本号 | VERSION `2.3.2` → `2.3.3`,build `1122` → `1160` |
| 变更规模 | 16 文件,+299/−29(全部为上游 v2.3.2..v2.3.3 内容) |
| 推送 | `11efb98..bb7e232` → `origin/main`(快进) |

## 2. 上游 v2.3.2 → v2.3.3 变更内容

1. **发布签名/entitlements 修复**(PR #628,Intellligent15)
   - `release.yml` +83:签名时保留应用 entitlements、校验带点 entitlement key
   - 新增 `tests/test_privacy_configuration.py`(+79 行)
2. **日历权限**(PR #630,GitCjp)
   - entitlements 新增 `personal-information.calendars`、`device.camera`,Apple Events 例外加 `com.apple.Notes`
3. **加载/Scheme 修复**(PR #658 bugfix/load-data)
   - pbxproj scheme 修复、release.yml 签名脚本调整
   - 蓝牙 HUD 动画资源查找增加 `BluetoothHUDAnimations` 子目录回退
4. **杂项**:LLM 定价数据更新(pricing.json)、CHANGELOG/appcast 归档

## 3. 本地改动保留情况(合并后逐项验证)

| 本地内容 | 状态 | 验证方式 |
|---|---|---|
| `matters.swift` 展开刘海最小宽度 420 | ✅ 保留 | 合并前后 diff 为空;产物反汇编确认 770/690/420 三常量 |
| 蓝牙 HUD 动画 PID 识别(devicePIDMap 等) | ✅ 保留 | 当前文件含全部代码 |
| 蓝牙动画资源(8 个 .mov + 5 个 Lottie JSON) | ✅ 保留 | 产物 Resources 完整 |
| `IdleAnimationView` 放大修复(393cc55) | ✅ 保留 | 与修复后版本 diff 为空 |

**合并零冲突**:每个文件来自某一侧完整版本,无混合产物。

## 4. 发现的历史修复丢失与恢复

### 4.1 空闲动画不显示 —— 已恢复 ✅

- **根因**:上游 v2.3.3 的 `ContentView.swift` 中,空闲动画分支前有一个**空 body 的 else-if**(条件几乎相同),空闲时先命中空分支,动画分支永远执行不到。
- **历史修复**:本地 fork 版本(`3e87e45^`)为单分支(`!expandingView.show && !isCurrentScreenExpansionVisible` + 动画 body)。
- **丢失时点**:`11efb98`(v2.3.2 合并,squash 用上游 ContentView.swift 替换本地版,-481 行)。
- **恢复方式**:合并为单分支(与历史修复一致),Debug 版重建验证,**用户实测动画正常**。

### 4.2 拓展屏不显示 —— 已移植,待实测 ⏳

- **现象确认**:连接拓展屏后,拓展屏上无动态岛(现象 A)。
- **历史修复**:`489ecd9`(窗口前置 orderFrontRegardless)、`1282e72`(display/invalidateShadow 刷新)。
- **丢失时点**:`11efb98`(DynamicIslandApp.swift 被上游替换)。
- **恢复方式**(核心 3 处):
  1. `screenConfigurationDidChange` → `adjustWindowPosition(changeAlpha: true)`
  2. 多屏分支定位后加 `orderFrontRegardless()` + `display()` + `invalidateShadow()`
  3. 单屏分支同样处理
- **验证状态**:编译通过、Debug 版运行中;**无稳定复现步骤,运行时验证待用户实测**。

### 4.3 评估后未移植

- **showOnAllDisplays 变更全清重建**(489ecd9):当前版 `cleanupWindows(shouldInvert:)` 按模式清理逻辑基本正确,全清重建收益有限且有重建失败风险,暂缓。

### 4.4 排查结论

- 本地 fork 历史中 13 个动画相关提交全部检查,除 4.1/4.2 外其余保留。
- 上游 v2.3.2..v2.3.3 未触碰 `matters.swift`、`IdleAnimationView.swift`,本地版本自动保留。

## 5. 验证过程记录

1. **合并结构**:`bb7e232` parents = `11efb98` + `246add1` ✓
2. **双向树对比**:
   - `diff v2.3.3 bb7e232` = 仅本地独有(.codegraph/assets/matters.swift)✓
   - `diff 11efb98 bb7e232` = 恰好上游 16 文件 ✓
3. **产物反汇编**:
   - `matters.o`:770.0/690.0/420.0 常量正确(0x4088…/0x4085…/0x407A…)✓
   - `SettingsView.o`:420.0 ×2,**无 640.0** ✓
   - 主二进制 movk 扫描:420×7、640(movz)×9(默认值等,非设置最小值)✓
4. **运行归属**:所有窗口属同一进程;md5 与签名(TeamIdentifier X53Y27JYRA)一致 ✓

## 6. 附:"设置 min 640" 之谜结论

- 用户曾看到设置界面最小宽度 640,与产物 420 矛盾。
- **结论**:该设置窗口来自 **Xcode DerivedData 中的旧 Debug 产物**(旧源码时代构建),非本次合并产物。
- Debug 版(独立 bundle id `com.Ebullioscopic.Atoll.dev`)实测 min 420 正确,证明合并/构建无误。

## 7. 构建与签名状态

| 版本 | 路径 | 签名 |
|---|---|---|
| Release v2.3.3 | `build/Build/Products/Release/Atoll.app` → `/Applications/Atoll.app` | Apple Development `623681153@qq.com`(手动重签) |
| Debug | `build_debug/Build/Products/Debug/Atoll.app` | Apple Development `623681153@qq.com`(手动重签) |

- 说明:Xcode 未登录该 Apple ID,自动 provisioning 不可用,故采用「ad-hoc 构建 + 手动 codesign」流程。
- Debug 版 bundle id 带 `.dev` 后缀,配置与 Release 隔离。

## 8. 待办

- [ ] 提交两个修复(空闲动画 + 拓展屏)到 main(当前为工作区修改)
- [ ] 重建 Release 版并同步 `/Applications`(当前仍为旧代码)
- [ ] 用户实测拓展屏修复(无稳定复现,遇 bug 时验证)
- [ ] 如需公网分发:Developer ID 证书 + notarization
